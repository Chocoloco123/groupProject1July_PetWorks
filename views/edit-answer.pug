extends layout.pug

include utils.pug

block content
    div(id='editAnswer-form' class='editAnswerFormContainer')
        +validationErrorSummary(errors)
        div(id='editAnswerContainment')
            div(id='editAnAnswer') Edit Answer:
            form(action=`/answers/${answer.id}/edit` method='post')
                input(type='hidden' value=questionId name='questionId')
                input(type='hidden' name='_csrf' value=csrfToken)
                +field('', 'answer', answer.answer, 'textarea')
                button(type='submit' class='regSubmitBtn') Submit
                a(href='/' class='.cancelBtn' id='editAnsCancel') Cancel